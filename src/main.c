#include <stdio.h>
#include "math_operations.h"

int main() {
    int sum = add(5, 3);
    int difference = subtract(5, 3);
    printf("Sum: %d\n", sum);
    printf("Difference: %d\n", difference);
    return 0;
} 